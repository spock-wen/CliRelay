# Review package: 38ed69ad..c719ad7d

## Commits
c719ad7d feat: 新增 NewOpenAICompatAnalyzer 识图器构造别名

## Files changed
 internal/vision/analyzer.go            |  6 ++++++
 internal/vision/analyzer_alias_test.go | 13 +++++++++++++
 2 files changed, 19 insertions(+)

## Diff
diff --git a/internal/vision/analyzer.go b/internal/vision/analyzer.go
index 313d896d..d649a8e4 100644
--- a/internal/vision/analyzer.go
+++ b/internal/vision/analyzer.go
@@ -56,20 +56,26 @@ func NewOpenCodeGoAnalyzer(baseURL, apiKey, model string) *OpenCodeGoAnalyzer {
 			Timeout: 30 * time.Second,
 			Transport: &http.Transport{
 				MaxIdleConns:       10,
 				IdleConnTimeout:    90 * time.Second,
 				DisableCompression: false,
 			},
 		},
 	}
 }
 
+// NewOpenAICompatAnalyzer 构造一个指向 OpenAI 兼容视觉模型的识图器。
+// 复用 OpenCodeGoAnalyzer 的 prompt 与 HTTP 逻辑，仅语义上区分调用目标。
+func NewOpenAICompatAnalyzer(baseURL, apiKey, model string) *OpenCodeGoAnalyzer {
+	return NewOpenCodeGoAnalyzer(baseURL, apiKey, model)
+}
+
 func (a *OpenCodeGoAnalyzer) Name() string { return "opencode-go" }
 
 func (a *OpenCodeGoAnalyzer) Analyze(ctx context.Context, req AnalyzeRequest) (AnalyzeResponse, error) {
 	var prompt string
 	if req.IsFollowUp {
 		prompt = a.buildFollowUpPrompt(req)
 	} else {
 		prompt = a.buildInitialPrompt()
 	}
 
diff --git a/internal/vision/analyzer_alias_test.go b/internal/vision/analyzer_alias_test.go
new file mode 100644
index 00000000..26310530
--- /dev/null
+++ b/internal/vision/analyzer_alias_test.go
@@ -0,0 +1,13 @@
+package vision
+
+import "testing"
+
+func TestNewOpenAICompatAnalyzer(t *testing.T) {
+	a := NewOpenAICompatAnalyzer("https://api.openai.com/v1", "sk-test", "gpt-4o")
+	if a == nil {
+		t.Fatal("expected non-nil analyzer")
+	}
+	if a.Name() == "" {
+		t.Fatal("expected analyzer to have a name")
+	}
+}
\ No newline at end of file
